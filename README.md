# pole12krd v4
Render: Language = Docker, Dockerfile Path = ./Dockerfile.
Environment Variables: OPENAI_API_KEY = secret key (never commit it), OPENAI_MODEL = a model available to your API account.
OpenAI API is not unlimited/free. Do not put API keys in GitHub or frontend files.
